/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost'],
      // Si también planeas usar imágenes de un servidor de producción, añádelo aquí
      // Por ejemplo: domains: ['localhost', 'mysite.com'],
    },
    // Otras configuraciones de Next.js que puedas necesitar
  }
  

export default nextConfig;
